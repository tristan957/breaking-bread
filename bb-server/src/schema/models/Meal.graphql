type Meal {
	id: Int!
	city: String! # TODO: Resolver to show just city, state
	relativeDistance: Int # TODO: Resolver for if user is logged in
	startTime: DateTime!
	endTime: DateTime!
	price: Float!
	title: String!
	description: String!
	imagePath: String!
	maxGuests: Int!
	createdAt: DateTime!
	updatedAt: DateTime!
	host: User!
	guests: [User!]!
	recipes: [Recipe!]!
	guestCount: Int!
	isFull: Boolean!
}
